function verificarNumero(numero) {
    if (numero > 0) {
        console.log(" Positivo ");
    } else if (numero > 0) {
       console.log(" Negativo ");
    } else {
        console.log(" Zero ")
    }
}
verificarNumero(2)